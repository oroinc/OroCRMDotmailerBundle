connector:
    name: oro_importexport
    jobs:
        dotmailer_new_contacts:
            title: "Import new Contacts from dotMailer"
            type:  import
            steps:
                -
                    title:     new_contact

{% import 'OroUIBundle::macros.html.twig' as UI %}

<div class="widget-content">
    <div class="row-fluid form-horizontal">
        <div class="responsive-block">
            {{ UI.renderProperty('orocrm.dotmailer.integration.label'|trans, entity.channel) }}
            {{ UI.renderProperty('orocrm.dotmailer.datafield.name.label'|trans, entity.name) }}
            {{ UI.renderProperty('orocrm.dotmailer.datafield.type.label'|trans, entity.type) }}
            {{ UI.renderProperty('orocrm.dotmailer.datafield.visibility.label'|trans, entity.visibility) }}
            {{ UI.renderProperty('orocrm.dotmailer.datafield.default_value.label'|trans, entity.defaultValue) }}
            {{ UI.renderHtmlProperty('orocrm.dotmailer.datafield.notes.label'|trans, entity.notes) }}
        </div>
    </div>
</div>
